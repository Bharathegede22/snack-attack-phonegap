- render_flash
- if @refunded
	:javascript
		var amt;
		var auto = setTimeout(function(){ submitRedirect(); }, 3000);
		function submitRedirect() {
			if($('.alert-success').text() == 'Your refund has been initiated and it should reach you in 5-15 days') {
				pushEvent('Wallet', 'Widget Refund Amount (My Bookings)', amt);
			}
			clearTimeout(auto);
			location.reload();
		}
- else
	- if current_user.wallet_total_amount<=0
		.size-18.t-c
			Your Wallet currently has no amount available for refund.
	- else	
		.size-18.t-c
			You can refund up to: 
			%span.f-b Rs. #{number_with_delimiter(current_user.wallet_total_amount)}

		%br/
		%br/

		%form{:id => "SearchForm", :onsubmit => "amt = $('#refundAmount').val();return SubmitForm(this,'/wallets/refund','ModalContent');", :action => "javascript:void(0)", "data-ajax" => "false"}
			
			.t-c.size-18
				Enter amount

				%input#refundAmount{name: "amount"}
			
			.t-c
				%input.button.m-t15{style: "width:120px;", type: "submit", value: "Refund"}
		- _15_day_booking = current_user.upcoming_bookings.select{|u| u.starts > Time.now && u.starts<Time.now.advance(days: 15)}
		- if _15_day_booking.any?
			.p-10.p-l20.p-r20.size-11.position-r{colspan: '2'}
				%i Are you sure you want to refund now? You have an upcoming booking which is using the Security Deposit in the Wallet. Keep in mind that it takes 5-15 days for the amount to reach you.
